# Derived Results

This folder contains compact derived summaries used to verify the manuscript tables and figures.

The files are summary-level CSV and Markdown reports. Prediction-level path panels, raw monthly records, and semi-raw flight tables are excluded.

